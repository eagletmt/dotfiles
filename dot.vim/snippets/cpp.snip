snippet inc
    #include <${1:header}>${0}

snippet main
    int main()
    {
        ${0}
        return 0;
    }

snippet for
    for (${1}::iterator ${2}(${3}.begin()); $2 != $3.end(); ++$2) {
        ${0}
    }

snippet cfor
    for (${1}::const_iterator ${2}(${3}.begin()); $2 != $3.end(); ++$2) {
        ${0}
    }

